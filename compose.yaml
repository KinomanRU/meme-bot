services:
    meme-bot:
        build:
            dockerfile: ./Dockerfile
            context: ./
        container_name: meme-bot
        image: meme-bot
        restart: unless-stopped
        environment:
            - BOT_TOKEN=
