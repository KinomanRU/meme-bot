services:
    meme-bot:
        build:
            dockerfile: ./Dockerfile
            context: ./
        container_name: meme-bot
        image: meme-bot
        restart: unless-stopped
        shm_size: 128mb
        environment:
            - BOT_TOKEN=
